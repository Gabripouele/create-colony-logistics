# Network and Chunk Sending

**PENDING CONTROLLED TRAVERSALS.** Server compression threshold is 256 bytes and native transport is enabled. Required comparisons are existing/new terrain at fixed low/high speed, with server packet preparation/compression/send queue, network throughput/latency, client receive, chunk application and mesh completion aligned by timestamp.

